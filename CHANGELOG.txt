31 Oct 2025 - initial version
01 Nov 2025 - Added LLVM-style RTTI to AST (Node/NodeKind, isa/dyn_cast), introduced SemanticAnalyzer with scope
              tracking, type checks, and strict GOTO/GOSUB validation (now default), enabled string-to-string
              comparisons (codegen via strcmp), refactored codegen/optimizer away from dynamic_cast, integrated
              semantics into the compile pipeline (CodeGenerator::setSemantics).
            - Added targeted file-stream lexer tests (std::ifstream) that write under build/tmp to cover
              Lexer(std::istream&) directly; confirmed main() and compileFile*/phase-log paths use stream-based
              lexing. Strengthened semantic analysis with IF target validation, numeric-only FOR start/end/step,
              and duplicate line number detection; added unit tests for each. Verified 'make clean lint test build
              demo' complete successfully.
            - Added math function calls to expressions:
            - Introduced CallExpr AST and parsing of identifier(...) with comma-separated args.
            - Implemented numeric functions SQR (sqrt) and ABS with semantic checks (unary, numeric-only).
            - Codegen now declares and emits calls to @sqrt and @fabs; added -lm when linking on non-Apple.
            - Unit tests cover codegen emission and semantic arity/type errors.
            - Extended math intrinsics per GW-BASIC manual (partial set):
            - Added SIN, COS, TAN, ATN, LOG, EXP, INT, FIX, SGN (numeric-only, arity=1) including domain checks
              for SQR/SQRT and LOG + string-comparison validation; codegen emits @sin/@cos/@tan/@atan/@log/@exp,
              @floor, and patterns for FIX/SGN.
            - New intrinsics: RND (uniform [0,1) via drand48), CINT (round), CSNG (float truncate/extend),
              CDBL (pass-through). Semantics enforce arity=1 and numeric args.
            - Codegen declares @drand48 and @round; CSNG lowers to fptrunc/fpext. CDBL is passthrough.
            - Added exhaustive happy/sad path tests across all math intrinsics: wrong-arity and string-arg
              SemanticError checks, plus domain-invalid value tests for SQR/SQRT and LOG. Added codegen pattern
              tests and e2e tests (including CINT half-away-from-zero and RND range assertions).
            - All unit/integration/e2e tests green; demo targets unchanged.
            - Implemented full RND(x) semantics and RANDOMIZE statement:
            - RND(x): x=0 returns last value; x<0 reseeds using x and returns next; x>0 returns next. Implemented
              via IR helper function @gwb_rnd and a global @gwb_last_rnd. Helper emitted only when used.
            - RANDOMIZE [expr]: Seeds RNG via srand48; without expr uses time(NULL). Added parser, semantics, and
              codegen support.
            - Tests: unit (semantics/codegen) and e2e validating repeat-on-zero, reseed-on-negative, advance-on-positive,
              and RANDOMIZE behaviors. All tests pass.
02 Nov 2025 - Control flow and loops expansion + demos
            - FOR: Added multi-line FOR...NEXT support.
              - New AST marker NextStmt; parseFor now permits newline before NEXT.
              - Parser folding pass aggregates intervening statements into ForStmt::body; validates NEXT var name.
              - Semantics analyze loop bodies; codegen unchanged for ForStmt bodies.
            - FOR STEP: Implemented ascending/descending loops.
              - Codegen chooses <= or >= based on step sign (select across fcmp results).
              - E2E tests cover descending loops and sad paths (no-iteration cases).
            - IF blocks: Added multi-line IF...THEN and IF...THEN...ELSE with END IF.
              - Tokens: ELSE. AST: IfBlockStmt, ElseStmt, EndIfStmt. Parser folding handles THEN/ELSE bodies.
              - Codegen emitIfBlock lowers then/else paths; semantics validate and analyze bodies.
              - Unit + E2E tests added for happy/sad paths.
            - WHILE: Added single-line and multi-line WHILE...WEND.
              - Tokens: WHILE, WEND. AST: WhileStmt (+ WendStmt marker). Parser folding handles multi-line bodies.
              - Codegen emitWhile generates cond/body/end; semantics validate condition and analyze bodies.
              - Unit + E2E tests added (inline, multi-line, and error cases).
            - Demos: Added multi-line loop examples and Fibonacci.
              - demos/trig-multi-line-loop.bas (multi-line FOR loop).
              - demos/fibonacci.bas (multi-line WHILE...WEND printing up to 100).
              - Makefile.d/demo.mk updated to build new demos.
            - Tooling/build: All targets green.
              - Verified 'make clean configure lint test build demo' succeeds.
            - Improved tests for scoping to ensure our semantic analysis is correct.
            - Constants + codegen cleanup:
              - Centralized character/string constants in include/basic_compiler/Symbols.h
                (CH_SPACE, SYM_DEL, SYM_LF, SYM_TAB, SYM_CR, CH_SINGLE_QUOTE, SYM_NULL, CH_DOLLARSIGN;
                 STR_LF, STR_DBL_QUOTE, STR_SPACE, STR_PERCENT) and wired Lexer/CodeGenerator to use them.
              - Replaced scattered literals in codegen/logging: '$'→CH_DOLLARSIGN, '\n'→SYM_LF, "\n"→STR_LF.
              - Adopted std::format for IR assembly to reduce string concatenation noise in:
                ensure_array_allocated.cpp, ensure_var_allocated.cpp, emit_expr.cpp, emit_line_block.cpp,
                emit_for.cpp, emit_while.cpp, emit_if_block.cpp, emit_subroutine_inline.cpp (and related paths).
            - Coverage and pipeline:
              - Coverage metric set to lines with COVERAGE_MIN=100 retained. Expanded include set to parser
                (node_name.cpp, consume.cpp) while continuing to exclude codegenerator/ from gating.
              - Ensured the sequence 'make clean configure lint test demo coverage' runs green; when run under
                parallel make, discovered 'clean' conflicts, so recommend sequential invocation for pipeline.
            - Semantics tests:
              - Added tiny AST-driven unit tests to exercise intrinsic helper paths indirectly via analyze()
                (unknown function errors, arity mismatch, LOG with comparison arg, SQR negative constant).
              - Keeps public API boundaries intact; no private hooks needed. All tests pass.
03 Nov 2025 - GW-BASIC extensions: memory ops, CALL/USR, DEF SEG/USR, CHDIR, ASC/CHR$/LEFT$/RIGHT$/MID$, DATA/READ,
              demos
            - DEF SEG + memory-mapped operations and USR/CALL
              - Parser/AST/Semantics: added DefSegStmt (DEF SEG[=expr]); BLOAD/BSAVE/POKE/CALL; DEF USR.
                - Validations: filenames must be strings; addresses/offsets/lengths numeric; DEF SEG numeric;
                  CALL/DEF USR address numeric.
              - Codegen: introduced emulated memory and segment globals: @gwb_mem (1 MiB) and @gwb_seg; declared libc
                funcs fopen/fclose/fread/fwrite; lowered PEEK/POKE/BSAVE/BLOAD against seg*16+offset addressing.
              - CALL: added @gwb_call(i64 %addr) helper; current behavior interprets opcode byte 1 at %addr to print
                "CALLED\n"; lowered CALL expr to compute effective address and invoke helper. USR(x) returns argument
                (identity) for now.
              - Tests: unit (semantics + codegen patterns), integration (presence of stdio calls), E2E (DEF SEG with
                PEEK/POKE/BSAVE/BLOAD/USR, CALL helper).

            - COLOR statement tests + robust codegen
            - Implemented unit/integration/e2e tests for COLOR.
              - Unit: parser variants (0–3 args), semantics numeric-only args, and codegen pattern (uses SGR printf).
              - Integration: IR contains @.fmt_sgr and @printf usage when COLOR present.
              - E2E: program using COLOR 2,4 prints ESC[32m and ESC[41m before text.
            - Fixed IR emission for COLOR to avoid invalid string literal lengths.
              - Replaced per-color ANSI C-string tables with:
                - A single SGR format string (@.fmt_sgr = "%c[%dm\0" as byte array).
                - Foreground/background integer lookup tables mapping PC palette indices to ANSI codes.
              - Codegen now indexes these tables and calls printf("%c[%dm", 27, code) for fg/bg.
            - All tests green: make clean configure lint test demo.

            - DATA/READ backend
              - Codegen emits @gwb_data (ptr table of string literals) and @gwb_data_idx; READ consumes sequentially,
                storing either pointers (string variables) or atof-converted numbers (numeric variables/arrays).
              - Tests extended to cover READ targets and ensure IR structures exist.

            - CHDIR statement
              - Parser/AST: ChdirStmt; Semantics enforce string path; Codegen calls libc chdir(ptr).
              - Tests: unit/integration/E2E (changes dir then BSAVE creates a file in new cwd).

            - ASC and CHR$ functions
              - Semantics: ASC expects string arg; CHR$ expects numeric; arity=1.
              - Codegen: ASC loads first byte of string; CHR$ allocates two bytes and writes char+NUL.
              - Tests: unit/integration/E2E verifying IR patterns and outputs ("A" and 65.000000).

            - CDBL coverage
              - Confirmed CDBL is pass-through in codegen; added integration smoke test while existing unit/E2E already
                covered it.

            - String functions LEFT$, RIGHT$, MID$
              - Added SemanticAnalyzer::isKnownStringFunction() and robust arity/type validation for string built-ins.
              - type_of() recognizes built-in string functions as string-typed.
              - Codegen: declared @strncpy; lowered
                - LEFT$(s$, n): malloc(n+1); strncpy; NUL-terminate.
                - RIGHT$(s$, n): compute offset = strlen(s)-n; malloc(n+1); strncpy from s+offset; NUL-terminate.
                - MID$(s$, start[, len]): 1-based start; compute s+(start-1); len omitted => strlen remainder;
                  malloc(len+1); strncpy; NUL-terminate.
              - Tests: unit semantics (type+arity), unit codegen (strncpy/malloc present), integration (IR contains
                @strncpy), E2E (expected results).

03 Nov 2025 - Demo updates and arrays/data demo
              - Rewrote demos/data-arrays-strings-integers-and-floats.bas to avoid array element expressions and
                FOR-body READs. Uses scalar READs then stores into arrays; prints scalar sums and string
                concatenations; compiles cleanly.
              - Added demo to Makefile.d/demo.mk and restored chain-level4 demo.

            - E2E working directory and mem.bin location
              - Adjusted DEF SEG e2e test to BSAVE/BLOAD "build/mem.bin" so artifacts land under build/ rather than
                repo root.
              - Left E2E test WORKING_DIRECTORY at project root to preserve relative paths for CHAIN/MERGE/RUN tests
                against demos/.

            - Codegen/runtime declarations
              - emit_header: now declares fopen/fclose/fprintf/fread/fwrite, chdir, strncpy; keeps
                printf/scanf/strcpy/strcat/malloc/strlen helpers.
              - Added .mode_rb/.mode_wb constants for binary I/O.

            - Parser/semantics/codegen cleanup
              - Centralized expression string typing predicate; extended isStringExpr for built-in string functions
                and DEF FN string returns.
              - Collected DATA/READ literal ids in semantics-driven collection to drive @gwb_data emission.

            - All pipelines green
              - Verified 'make clean configure lint test demo' completes successfully on macOS (arm64-apple-macOS)
                with Ninja/Clang 17.

          - SCREEN function and virtual screen buffer
            - Implemented SCREEN(row,col[,z]) as a numeric intrinsic returning the ASCII code (0–255) at the given
              1-based coordinates.
              - Semantics: recognized as builtin; accepts 2 or 3 numeric args; third arg reserved (ignored for now).
              - Codegen: reads from a new 80x25 virtual screen buffer @gwb_screen; clamps indices to bounds.
            - Mirrored PRINT output to the virtual screen:
              - Declared @snprintf and added IR helper @gwb_screen_write(ptr,len) to update @gwb_screen and cursor.
              - PRINT to stdout now formats into @gwb_sbuf and calls @gwb_screen_write; file channels (#n) are
                unaffected.
              - New globals: @gwb_screen (2000 bytes), @gwb_cur_row/@gwb_cur_col, @gwb_sbuf (256 bytes).
            - Tests added:
              - Unit: test_semantics_screen_function.cpp (arity/type), test_codegen_screen_ir.cpp (globals/helper
                present).
              - E2E: test_screen_e2e.cpp validates ASCII codes after printing "ABC".

05 Nov 2025 - ON GOTO/GOSUB, LLVM 17 pin, lint fixes, and editor config
            - Language: ON GOTO / ON GOSUB
              - Lexer: added KwOn token.
              - Parser: ON <expr> GOTO l1[,l2...] and ON <expr> GOSUB l1[,l2...] via parseOnGotoGosub().
              - AST: new nodes OnGotoStmt and OnGosubStmt (index expr + targets).
              - Semantics: index must be numeric; validates target lines (errors in strict mode; warnings otherwise).
              - Codegen: lowers to a switch i32 on 1-based index; default falls through.
                - ON GOTO: cases branch to line labels; default → continuation label.
                - ON GOSUB: cases branch to per-case entry labels and inline subroutine; common continuation label.
                - Supported in top-level lines, IF/ELSE bodies, and WHILE bodies.
              - Tests:
                - Unit: parser (OnGoto/OnGosub), semantics (string index errors, missing targets).
                - Integration: IR patterns for switch targets and default labels; verifies continuation label blocks.
                - E2E: correct branch/subroutine execution and out-of-range (0 and >N) fallthrough behavior.

            - Tooling/CI: pin LLVM 17 everywhere and fix clang-tidy in Docker lint
              - Dockerfile: install pinned LLVM/Clang 17 (clang-17, clang-tidy-17, llvm-17-tools, libc++-17-dev,
                libc++abi-17-dev, lld-17); export PATH/CC/CXX for LLVM 17.
              - CMake toolchain (cmake/toolchain.cmake):
                - macOS: require Homebrew llvm@17 explicitly; fail fast if missing.
                - Linux: pin to /usr/lib/llvm-17 (no multi-version probing); set PATH, LLVM_PREFIX, and rpaths.
              - Build config (cmake/BuildConfig.cmake): apply -stdlib=libc++ only for C++ compilation to avoid
                clang-tidy treating '-stdlib=libc++' as an error on C sources; link with libc++ for C++.
              - GitHub Actions coverage workflow: macOS installs llvm@17; exports LLVM_PREFIX/CC/CXX/PATH;
                Ubuntu uses apt.llvm.org llvm.sh 17 path; both use unified toolchain.
              - Make lint/linux now succeeds (clang-tidy present and clean) and make test/linux passes.

            - Style/tooling: .editorconfig compatibility improvements
              - Split grouped sections for compatibility with older plugins:
                - Replace [{Makefile,*.mk}] with [Makefile] and [*.mk].
                - Replace [{CMakeLists.txt,*.cmake}] with [CMakeLists.txt] and [*.cmake].
            - ERASE statement + apostrophe comments tests + diagnostics
            - ERASE arrays: full pipeline implementation and tests
              - Lexer: added ERASE keyword.
              - AST: new EraseStmt node with list of array names.
              - Parser: parseErase() for ERASE name[,name...]; wired into keyword dispatch.
              - Semantics: ERASE removes arrays from the active environment; added historical dims map so codegen
                can compute lengths for reset without re-enabling usage. Using an array after ERASE now yields a
                clear error message: "TypeError: array '<name>' not DIM'd".
              - Codegen: ERASE emits stores to reset arrays (numeric → 0/0.0; string → null); respects OPTION BASE
                when computing extents; uses existing ensureArrayAllocated helpers.
              - Tests:
                - Unit: parser coverage (ERASE name list), semantics positive (ERASE + re-DIM allows reuse), and
                  negative (PRINT A(1) after ERASE A raises "array not DIM'd").
                - Integration: IR patterns show zeroing/null stores for affected arrays.
                - E2E: ERASE followed by re-DIM compiles/runs (no output), validating behavior.

            - Apostrophe (') inline comment support: strict tests added
              - Lexer already treated apostrophe as comment to EOL; added tests to guarantee strict behavior.
              - Integration: confirms garbage tokens after apostrophe are ignored (no lex/parse errors); IR contains
                expected print formatting for statements before comments.

06 Nov 2025 - DATA/READ/RESTORE overhaul
            - Lexer/Parser/AST: DATA items now preserve tokenization as quoted strings vs. numbers.
              - New AST type DataItem { bool isString; std::string text } and DataStmt now holds vector<DataItem>.
              - Parser::parseData produces DataItem entries; existing unit tests updated accordingly.
            - Codegen: Removed atof-based numeric parsing in READ; now follows GW-BASIC tokenization.
              - Globals: emit @gwb_data (ptrs to string payloads), @gwb_data_isstr (i8 flags), @gwb_data_num (double values).
              - READ: bounds-checks @gwb_data_idx, enforces that numeric targets cannot consume quoted-string items
                (runtime error dispatched via ON ERROR if installed), and loads numeric values from @gwb_data_num.
                String targets continue to store pointers to literal payloads.
              - Scalar numeric stores now use typed stores via storeNumberToVar; array elements use storeNumberToArrayElem.
              - RESTORE: lowers to a store of 0 into @gwb_data_idx (no operand variant only); RESTORE <line> is not supported.
              - Out-of-data: attempting to READ past the end of the DATA table triggers runtime error code 9 and dispatches via ON ERROR when installed.
              - IR: removed declaration and all uses of @atof; READ no longer depends on libc conversion.
            - Tests:
              - Unit: updated DATA parser test to assert isString/text; existing READ/RESTORE parser tests remain green.
              - Integration: new tests verify presence of @gwb_data_isstr/@gwb_data_num and absence of @atof; RESTORE emits data_idx reset.
              - E2E: added coverage for
                - RESTORE rewinding behavior (READ after RESTORE yields from start),
                - type-mismatch on reading quoted strings into numeric targets (handler prints), and
                - out-of-data handling via ON ERROR.
            - All suites green locally with artifacts under build/ via 'make clean configure'.
              - E2E: program with apostrophe comments runs and prints expected results for statements before comments.
              - Unit: added test ensuring a literal apostrophe inside a double-quoted string (e.g., "Bob's") is not
                treated as a comment and remains in the StringExpr.

            - Diagnostics
              - Improved semantic error message for call-like identifiers with arguments that are not builtins or
                user functions to treat them as array uses, yielding "array '<name>' not DIM'd" instead of
                "Unknown function" when applicable (e.g., after ERASE).

            - Array features (consolidated)
              - DIM arrays with types and multidimensional support
                - Numeric arrays by suffix: % (Int16→i32), & (Long32→i64), ! (Single→float), # (Double→double).
                - String arrays by suffix $: arrays of ptr elements.
                - Multidimensional DIM A(m,n,...) lowers to a single alloca of product extents.
                - OPTION BASE 0/1 honored for extent calculation and indexing (ub - base + 1).
              - Usage and type checking in semantics
                - Arity: number of indices must match number of DIM bounds.
                - Index expressions must be numeric; string indices rejected.
                - Assignments type-checked: numeric arrays accept numeric; string arrays accept string only.
                - Referencing arrays before DIM or after ERASE produces "array '<name>' not DIM'd".
              - Code generation
                - Ensures per-array stack allocation on first use (numeric/string variants) and reuses allocas.
                - Linearization from multi-index to linear element with GEP; prints use typed formats.
                - Numeric stores emit fptosi/fptrunc/bit-casts as appropriate for target element type.
                - String array elements are stored as pointers; string operations use @strncpy/@malloc helpers.
              - Runtime behavior helpers
                - CLEAR resets arrays seen before the CLEAR line: numeric to 0/0.0; strings to null.
                - ERASE resets only the specified arrays (numeric→0/0.0; strings→null) without re-enabling usage.
                - READ into arrays processes DATA in order; numeric via @atof; string items stored as pointers.
                - WRITE #n from string arrays lowers to fprintf with format constants.
                - Optionally mirrors runtime bounds errors into ERR/ERL and respects ON ERROR handlers
                  (ArrayBoundsTriggersOnErrorHandler e2e test).

            - Tooling
              - Linter scope narrowed to C++ sources to avoid environment-specific C toolchain header detection
                issues during clang-tidy in this sandbox; no functional code changes.

            - Consumed enough caffeine to make Mötley Crüe seem tame.

            - SWAP statement (variables and arrays)

            - Language: SWAP x, y
              - Lexer: added SWAP keyword.
              - AST: new SwapStmt with left/right variable references (scalar or array element) using ReadTarget.
              - Parser: SWAP varref, varref parsing wired in tryParseOtherKeywords.
              - Semantics: validates both refs exist (arrays DIM'd; indices numeric and correct arity) and types match
                (both numeric or both string). Clear error messages on mismatch.
              - Codegen: emits efficient swap for
                - Strings: load ptr from each side and store cross.
                - Numerics: load each as double with proper casts, then typed stores back to each destination
                  (vars or array elements). Array element refs include runtime bounds checks matching assignment/expr.

            - Tests: unit (parser + semantics), integration (IR patterns for string vars and numeric array elems),
              and e2e (numeric and string swaps produce expected outputs). All tests pass locally.

            - Numeric literal formats
                - Lexer: expanded number scanning to support scientific notation with E/D exponents (case-insensitive),
                  normalizing 'D' to 'E' for downstream parsing. Added '&' numeric forms beyond hex:
                  - &H[0-9A-F]+ (hex), &O[0-7]+ (octal), &[0-7]+ (octal shorthand), &B[01]+ (binary).
                  - Emits Integer tokens with decimal lexemes for base-prefixed forms.
                - Parser/Codegen: no changes required beyond lexer normalization; primary numeric parsing and DATA number
                  table construction now accept scientific notation with 'D.'
                - Tests: unit (lexer exponent normalization and &O/&/&B decoding), integration (DATA 1D2 → double
                  1.000000e+02 in @gwb_data_num), and E2E (PRINT &H/&O/&/&B and 1D2 produce expected values). All
                  tests are green.
            - Implement Exponentiation `^` operator, associated unit/integration, and e2e tests, then ensure the
              tests are all green.
            - Implement logical operators (AND/OR/NOT) for numeric truthy semantics, unit/integration/e2e tests, and
              ensure all tests are green.
            - Ensure relational operators are supported with strict semantics, unit/integration/e2e tests, and ensure
              these tests are green.

